/**
 * 渐变文字生成器 - 逻辑层入口
 *
 * 纯前端离线工具：输入多行文本 + 颜色停靠点，生成 Minecraft 兼容渐变文字，
 * 支持 19 种输出格式（Vanilla / MiniMessage / JSON / BBCode 等）。
 */
export * from './types'
export * from './core'
export * from './formats'
export * from './storage'
